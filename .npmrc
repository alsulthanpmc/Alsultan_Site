# PNPM Configuration
# Auto-install peers
auto-install-peers=true

# Save exact versions (remove ^ from version numbers)
save-exact=false

# Update check behavior
update-notifier=true

# Audit level for installs
audit-level=moderate

# Lockfile settings
lockfile=true
